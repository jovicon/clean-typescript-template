export const addTwoNumbers = (a: number, b: number): number => a + b;

export const subtractTwoNumbers = (a: number, b: number): number => a - b;

// TODO: Tests
export const multiplyTwoNumbers = (a: number, b: number): number => a * b;
